\definecolor{escpblue}{HTML}{240085}
\definecolor{firebrick}{HTML}{B22222}

% named=... is the only form that survives the default colour theme Quarto applies.
\usecolortheme[named=escpblue]{structure}
\setbeamertemplate{navigation symbols}{}
